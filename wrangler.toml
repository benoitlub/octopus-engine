# Deployed via .github/workflows/deploy-worker.yml on push to main
# (Cloudflare API token/account id added to repo secrets).
name = "octopus-engine"
main = "src/worker.ts"
compatibility_date = "2024-11-01"
